# Free LMS (React + Firebase + Vercel)

A fully free LMS system built with:
- React (Vite)
- Firebase Auth + Firestore
- Vercel hosting

Edit `src/firebase.js` to load Firebase values from environment variables.
